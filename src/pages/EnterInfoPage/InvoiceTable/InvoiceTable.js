import classNames from "classnames/bind";

import styles from "./InvoiceTable.module.scss";
const cx = classNames.bind(styles);
function InvoiceTable() {
  return <div className={cx('table')}>
    
  </div>;
}

export default InvoiceTable;
