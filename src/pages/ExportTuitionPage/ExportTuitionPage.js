import classNames from "classnames";

import styles from './ExportTuitionPage.module.scss';

const cx = classNames.bind(styles);

function ExportTuitionPage() {
    return ( <>Export Tuition Page</> );
}

export default ExportTuitionPage;